import 'package:flutter/material.dart';

class AppIcons {
  static const Icon tickMark = Icon(
    IconData(0xe1f6, fontFamily: 'MaterialIcons'),
    color: Colors.white,
  );

  static const Icon home = Icon(
    Icons.home,
  );
  static const Icon payment = Icon(
    Icons.payments,
  );
  static const Icon transfer = Icon(
    Icons.price_change,
  );
  static const Icon cards = Icon(
    Icons.credit_card,
  );
  static const Icon services = Icon(
    Icons.home_repair_service,
  );
}
